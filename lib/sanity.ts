/**
 * Tiny pure helper exposed as the first production function.
 * Used by the sanity smoke test so coverage has real statements to measure.
 */
export function add(a: number, b: number): number {
  return a + b;
}
